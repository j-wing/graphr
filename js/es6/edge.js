export class Edge {
	/* Constructors */
	constructor(fromNode, toNode, weight) {
		this.fromNode = fromNode
		this.toNode = toNode
		this.weight = weight
	}

	/* Render methods */
	render() { }
	drawLine() { }
}